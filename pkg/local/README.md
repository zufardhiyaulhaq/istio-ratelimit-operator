# Local Ratelimit Package
package directory for Istio Local Rate Limit. Contain two directory:
- `config` for building EnvoyFilter for `LocalRateLimitConfig`
- `ratelimit` for building EnvoyFilter for `LocalRateLimit`

